# Host training / export environment (CPU or CUDA).
# For CUDA, install the matching torch build from pytorch.org instead of the CPU wheel.
torch>=2.2
torchvision>=0.17
ultralytics>=8.3
onnx>=1.15
onnxruntime>=1.17
onnxscript>=0.1
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0
pillow>=10.0
pytest>=8.0

# ON-TARGET / QUANTIZATION (NOT pip-installable here):
#   vai_q_pytorch (pytorch_nndct)  -- Vitis AI 3.5 pytorch docker
#   vart, xir                      -- ZCU104 PetaLinux runtime
